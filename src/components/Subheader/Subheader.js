import React from "react";
import "./Subheader.css";

const Subheader = (props) => (
  <section className="subheader">
    <p>Simplified copy of the <a target="_blank" rel="noopener noreferrer" href="https://pokeapi.co/">PokeAPI</a>. This copy is to be used by programming students first learning about API's</p>
  </section>
)

export default Subheader;
